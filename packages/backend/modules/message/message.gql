type Message {
  id: ID!
  value: String!
  clientId: ID!
  client: Client
  createdAt: DateTime!
}

extend type Client {
  messages: [Message!]
}

input MessageCreate {
  value: String!
  clientId: ID!
}

type Mutation {
  createMessage(data: MessageCreate!): Message!
}
